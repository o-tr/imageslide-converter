export type PageSize = {
  width: number;
  height: number;
};

export type CanvasSize = {
  width: number;
  height: number;
};

export type PixelRect = {
  x: number;
  y: number;
  w: number;
  h: number;
};
